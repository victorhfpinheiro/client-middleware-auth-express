# Client Middleware Auth Express

Para rodar esse projeto basta fazer o clone na sua maquina e executar os comandos abaixo:

Para instalar as dependências:<br>

`yarn install`

Para executar o servidor:<br>

`yarn dev`
